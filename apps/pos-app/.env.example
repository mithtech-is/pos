# Backend URL the sync worker talks to
VITE_BACKEND_URL=http://localhost:9000

# Device code (must be registered in backend first)
VITE_DEVICE_CODE=POS001

# Default sync interval in ms (30s)
VITE_SYNC_INTERVAL_MS=30000
